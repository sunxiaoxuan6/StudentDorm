<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ADDRoomActivity"></activity>
        <activity android:name=".ExamineActivity" />
        <activity android:name=".RevisePasswordActivity" />
        <activity android:name=".StayActivity" />
        <activity android:name=".RoomActivity" />
        <activity android:name=".AllocationsActivity" />
        <activity android:name=".ExchangeActivity" />
        <activity android:name=".QueryActivity" />
        <activity android:name=".EnteringActivity" />
        <activity android:name=".OutActivity" />
        <activity android:name=".SupervisorActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".DormActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>